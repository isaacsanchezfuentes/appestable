# PostgreSQL
DATABASE_URL=postgresql://postgres:postgres123@localhost:5432/appestable

# Auth0
AUTH0_DOMAIN=dev-zbne73xs48twrr2a.us.auth0.com
AUTH0_AUDIENCE=https://appestable-api

# CORS (coma-separado; * = todos)
CORS_ORIGINS=*

# Logging: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Solo para tests locales — NUNCA en producción
DISABLE_AUTH=false