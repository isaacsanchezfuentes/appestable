package com.example.appestable.data.remote.api

interface AuthApi {

    suspend fun login(
        request: LoginRequest
    ): LoginResponse

    suspend fun logout()

    suspend fun refreshToken(
        refreshToken: String
    ): LoginResponse
}